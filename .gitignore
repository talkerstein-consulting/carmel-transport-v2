# dependencies
node_modules/
.pnp
.pnp.js

# build output
dist/
dist-ssr/
build/
*.local

# secrets — ReactBits Pro license key lives here
.env
.env.*
!.env.example

# editor / OS
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# scratch
/tmp/

# Source footage for the scroll scrubs — ~47MB, kept out of git.
# The rendered WebP sequences in app/public/seq/ are committed, so the site
# builds and deploys without these. They are only needed to re-run
# app/scripts/build-frames.sh when the footage changes.
*.mp4
*.mov

# superseded static build, replaced by the React app in app/
/site/
